camera:
  # Use device_id (int) for camera, or source (string path) for video file
  source: "data/in_video.mp4"
  device_id: 0
  resolution:
    width: 640
    height: 480
  fps: 15
  frame_timeout_ms: 2000
system:
  log_level: INFO
  log_file: logs/dms.log
    
